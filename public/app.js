const io=new IntersectionObserver(es=>es.forEach(e=>e.isIntersecting&&e.target.classList.add("show")),{threshold:.12});document.querySelectorAll(".reveal").forEach(x=>io.observe(x));
(function(){const reduceMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;if(reduceMotion)return;document.querySelectorAll("[data-tilt]").forEach(el=>{let raf=null;el.addEventListener("mousemove",e=>{const r=el.getBoundingClientRect();const px=(e.clientX-r.left)/r.width-.5,py=(e.clientY-r.top)/r.height-.5;if(raf)cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{el.style.transform=`perspective(900px) rotateX(${(-py*10).toFixed(2)}deg) rotateY(${(px*12).toFixed(2)}deg) translateZ(10px)`})});el.addEventListener("mouseleave",()=>{if(raf)cancelAnimationFrame(raf);el.style.transform="perspective(900px) rotateX(0) rotateY(0) translateZ(0)"})})})();
(function(){const reduceMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;const hero=document.querySelector(".hero-copy");if(!hero||reduceMotion)return;window.addEventListener("mousemove",e=>{const x=(e.clientX/window.innerWidth-.5),y=(e.clientY/window.innerHeight-.5);hero.style.transform=`perspective(1200px) rotateY(${(x*3).toFixed(2)}deg) rotateX(${(-y*3).toFixed(2)}deg)`},{passive:true})})();
const regForm=document.getElementById("regForm");
if(regForm){regForm.addEventListener("submit",async e=>{e.preventDefault();const f=new FormData(e.target),data=Object.fromEntries(f),r=document.getElementById("result"),btn=regForm.querySelector("button[type=submit]");r.textContent="Submitting...";btn.disabled=true;try{const x=await fetch("/api/register",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const j=await x.json();if(!x.ok)throw new Error(j.message||"Registration failed.");const params=new URLSearchParams({id:j.id,team:j.teamName,leader:j.leader});window.location.href=`/registration-success?${params}`;}catch(err){r.textContent=err.message;}finally{btn.disabled=false;}})}
